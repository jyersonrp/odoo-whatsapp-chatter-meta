<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Search View -->
    <record id="view_whatsapp_message_search" model="ir.ui.view">
        <field name="name">whatsapp.message.search</field>
        <field name="model">whatsapp.message</field>
        <field name="arch" type="xml">
            <search string="Search WhatsApp Messages">
                <field name="name"/>
                <field name="wamid"/>
                <field name="sender"/>
                <field name="recipient"/>
                <field name="partner_id"/>
                <field name="res_model"/>
                <filter string="Inbound" name="filter_inbound" domain="[('direction', '=', 'inbound')]"/>
                <filter string="Outbound" name="filter_outbound" domain="[('direction', '=', 'outbound')]"/>
                <separator/>
                <filter string="Failed" name="filter_failed" domain="[('status', '=', 'failed')]"/>
                <filter string="Delivered/Read" name="filter_delivered" domain="[('status', 'in', ('delivered', 'read'))]"/>
                <group>
                    <filter string="Direction" name="group_direction" context="{'group_by': 'direction'}"/>
                    <filter string="Status" name="group_status" context="{'group_by': 'status'}"/>
                    <filter string="Contact" name="group_partner" context="{'group_by': 'partner_id'}"/>
                    <filter string="Date" name="group_date" context="{'group_by': 'date:day'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- List View -->
    <record id="view_whatsapp_message_tree" model="ir.ui.view">
        <field name="name">whatsapp.message.list</field>
        <field name="model">whatsapp.message</field>
        <field name="arch" type="xml">
            <list string="WhatsApp Message Logs" create="false" edit="false">
                <field name="date"/>
                <field name="name"/>
                <field name="wamid" optional="hide"/>
                <field name="direction" 
                       widget="badge" 
                       decoration-info="direction == 'outbound'" 
                       decoration-success="direction == 'inbound'"/>
                <field name="sender"/>
                <field name="recipient"/>
                <field name="partner_id"/>
                <field name="res_model" optional="show"/>
                <field name="res_id" optional="show"/>
                <field name="status" 
                       widget="badge"
                       decoration-danger="status == 'failed'"
                       decoration-success="status in ('delivered', 'read')"
                       decoration-info="status == 'sent'"
                       decoration-primary="status == 'received'"/>
                <field name="error_code" optional="show"/>
            </list>
        </field>
    </record>

    <!-- Form View -->
    <record id="view_whatsapp_message_form" model="ir.ui.view">
        <field name="name">whatsapp.message.form</field>
        <field name="model">whatsapp.message</field>
        <field name="arch" type="xml">
            <form string="WhatsApp Message Audit" create="false" edit="false">
                <sheet>
                    <div class="oe_title">
                        <label for="name"/>
                        <h1>
                            <field name="name"/>
                        </h1>
                    </div>
                    <group>
                        <group string="Delivery Details">
                            <field name="wamid"/>
                            <field name="date"/>
                            <field name="direction"/>
                            <field name="status" 
                                   widget="badge"
                                   decoration-danger="status == 'failed'"
                                   decoration-success="status in ('delivered', 'read')"
                                   decoration-info="status == 'sent'"
                                   decoration-primary="status == 'received'"/>
                            <field name="error_code" invisible="status != 'failed'"/>
                            <field name="error_message" invisible="status != 'failed'"/>
                        </group>
                        <group string="Parties and Document">
                            <field name="account_id"/>
                            <field name="sender"/>
                            <field name="recipient"/>
                            <field name="partner_id"/>
                            <field name="res_model"/>
                            <field name="res_id"/>
                            <field name="attachment_id" invisible="not attachment_id"/>
                            <field name="media_id" invisible="not media_id"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Message Content">
                            <field name="body" readonly="1" widget="text"/>
                        </page>
                        <page string="Raw JSON Payload" groups="base.group_no_one">
                            <field name="raw_payload" readonly="1"/>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Action -->
    <record id="action_whatsapp_message" model="ir.actions.act_window">
        <field name="name">Message Logs</field>
        <field name="res_model">whatsapp.message</field>
        <field name="view_mode">list,form</field>
        <field name="search_view_id" ref="view_whatsapp_message_search"/>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                History and audit log of dispatched and received transactional WhatsApp messages.
            </p>
        </field>
    </record>
</odoo>
